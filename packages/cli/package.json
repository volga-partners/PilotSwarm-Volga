{
  "name": "pilotswarm-cli",
  "version": "0.1.15",
  "description": "Terminal UI for PilotSwarm.",
  "type": "module",
  "bin": {
    "pilotswarm": "./bin/tui.js",
    "pilotswarm-cli": "./bin/tui.js"
  },
  "main": "./src/index.js",
  "exports": {
    ".": "./src/index.js",
    "./portal": "./src/portal.js"
  },
  "scripts": {
    "build": "echo 'pilotswarm-cli: no build step (plain JS)'",
    "prepack": "node -e \"const fs=require('fs');const cp=(s,d)=>{fs.mkdirSync(d,{recursive:true});for(const e of fs.readdirSync(s,{withFileTypes:true})){const sp=s+'/'+e.name,dp=d+'/'+e.name;e.isDirectory()?cp(sp,dp):fs.copyFileSync(sp,dp)}};for(const p of ['pilotswarm-ui-core','pilotswarm-ui-react']){const src='../'+p.replace('pilotswarm-','');const dst='node_modules/'+p;fs.rmSync(dst,{recursive:true,force:true});fs.mkdirSync(dst,{recursive:true});fs.copyFileSync(src+'/package.json',dst+'/package.json');fs.copyFileSync(src+'/README.md',dst+'/README.md');cp(src+'/src',dst+'/src')}\"",
    "start": "node ./bin/tui.js local --env ../../.env.remote",
    "start:remote": "node ./bin/tui.js remote --env ../../.env.remote",
    "clean": "true"
  },
  "keywords": [
    "copilot",
    "pilotswarm",
    "tui",
    "agent",
    "cli"
  ],
  "author": "Affan Dar",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/affandar/PilotSwarm"
  },
  "homepage": "https://github.com/affandar/PilotSwarm",
  "bugs": {
    "url": "https://github.com/affandar/PilotSwarm/issues"
  },
  "dependencies": {
    "ink": "^6.8.0",
    "pilotswarm-sdk": "^0.1.15",
    "pilotswarm-ui-core": "0.1.0",
    "pilotswarm-ui-react": "0.1.0",
    "react": "^19.2.4"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "bundledDependencies": [
    "pilotswarm-ui-core",
    "pilotswarm-ui-react"
  ],
  "files": [
    "bin/**/*",
    "src/**/*",
    "tui-splash.txt",
    "plugins/**/*",
    "README.md"
  ]
}
