# Diff for migration 0004

Migration file: `cms-migrations.ts` — `migration_0004_stored_procedures`

## Table Changes

None.

## New Indexes

None.

## Function Changes

All functions are new — no baselines exist. Every CMS data-access query that was previously inline SQL in `PgSessionCatalogProvider` is now a stored procedure.

### `cms_create_session` — new

```diff
@@
+ CREATE OR REPLACE FUNCTION SCHEMA.cms_create_session(
+     p_session_id TEXT, p_model TEXT, p_parent_session_id TEXT,
+     p_is_system BOOLEAN, p_agent_id TEXT, p_splash TEXT
+ ) RETURNS VOID
```

- Inserts into `sessions` with ON CONFLICT upsert (restores soft-deleted sessions).
- Seeds a zeroed `session_metric_summaries` row (ON CONFLICT DO NOTHING).

### `cms_update_session` — new

```diff
@@
+ CREATE OR REPLACE FUNCTION SCHEMA.cms_update_session(
+     p_session_id TEXT, p_updates JSONB
+ ) RETURNS VOID
```

- Accepts a JSONB object with optional camelCase keys.
- Uses `p_updates ? 'key'` checks to conditionally SET each column.
- Always sets `updated_at = now()`.

### `cms_soft_delete_session` — new

```diff
@@
+ CREATE OR REPLACE FUNCTION SCHEMA.cms_soft_delete_session(
+     p_session_id TEXT
+ ) RETURNS VOID
```

- Guards against deleting system sessions (`RAISE EXCEPTION` if `is_system = TRUE`).
- Sets `deleted_at` on both `sessions` and `session_metric_summaries`.

### `cms_list_sessions` — new

```diff
@@
+ CREATE OR REPLACE FUNCTION SCHEMA.cms_list_sessions()
+ RETURNS SETOF SCHEMA.sessions
```

- Returns non-deleted sessions ordered by `updated_at DESC`.

### `cms_get_session` — new

```diff
@@
+ CREATE OR REPLACE FUNCTION SCHEMA.cms_get_session(p_session_id TEXT)
+ RETURNS SETOF SCHEMA.sessions
```

- Returns 0 or 1 rows for the given session ID (non-deleted only).

### `cms_get_descendant_session_ids` — new

```diff
@@
+ CREATE OR REPLACE FUNCTION SCHEMA.cms_get_descendant_session_ids(p_session_id TEXT)
+ RETURNS TABLE (session_id TEXT)
```

- Recursive CTE traversing `parent_session_id` to find all descendants.
- Filters out soft-deleted sessions.

### `cms_get_last_session_id` — new

```diff
@@
+ CREATE OR REPLACE FUNCTION SCHEMA.cms_get_last_session_id()
+ RETURNS TEXT
```

- Returns the most recently active non-system session ID.
- Orders by `last_active_at DESC NULLS LAST`, LIMIT 1.

### `cms_record_events` — new

```diff
@@
+ CREATE OR REPLACE FUNCTION SCHEMA.cms_record_events(
+     p_session_id TEXT, p_events JSONB, p_worker_node_id TEXT
+ ) RETURNS VOID
```

- Accepts a JSONB array of `{eventType, data}` objects.
- Uses `jsonb_array_elements` to unpack and batch-insert into `session_events`.

### `cms_get_session_events` — new

```diff
@@
+ CREATE OR REPLACE FUNCTION SCHEMA.cms_get_session_events(
+     p_session_id TEXT, p_after_seq BIGINT, p_limit INT
+ ) RETURNS SETOF SCHEMA.session_events
```

- When `p_after_seq > 0`: returns events after that sequence, ordered ASC.
- Otherwise: returns the last N events in chronological order (subquery + re-sort).

### `cms_get_session_events_before` — new

```diff
@@
+ CREATE OR REPLACE FUNCTION SCHEMA.cms_get_session_events_before(
+     p_session_id TEXT, p_before_seq BIGINT, p_limit INT
+ ) RETURNS SETOF SCHEMA.session_events
```

- Returns events before the given sequence number, ordered ASC.

### `cms_get_session_metric_summary` — new

```diff
@@
+ CREATE OR REPLACE FUNCTION SCHEMA.cms_get_session_metric_summary(p_session_id TEXT)
+ RETURNS SETOF SCHEMA.session_metric_summaries
```

- Returns 0 or 1 summary rows for the given session.

### `cms_get_session_tree_stats` — new

```diff
@@
+ CREATE OR REPLACE FUNCTION SCHEMA.cms_get_session_tree_stats(p_session_id TEXT)
+ RETURNS TABLE (
+     session_count INT, total_tokens_input BIGINT, total_tokens_output BIGINT,
+     total_tokens_cache_read BIGINT, total_tokens_cache_write BIGINT,
+     total_dehydration_count INT, total_hydration_count INT,
+     total_lossy_handoff_count INT, total_snapshot_size_bytes BIGINT
+ )
```

- Recursive CTE on `session_metric_summaries` traversing `parent_session_id`.
- Aggregates token counts, lifecycle counts, and snapshot sizes across the tree.

### `cms_get_fleet_stats_by_agent` — new

```diff
@@
+ CREATE OR REPLACE FUNCTION SCHEMA.cms_get_fleet_stats_by_agent(
+     p_include_deleted BOOLEAN, p_since TIMESTAMPTZ
+ ) RETURNS TABLE (
+     agent_id TEXT, model TEXT, session_count INT,
+     total_snapshot_size_bytes BIGINT, total_dehydration_count INT,
+     total_hydration_count INT, total_lossy_handoff_count INT,
+     total_tokens_input BIGINT, total_tokens_output BIGINT
+ )
```

- GROUP BY `agent_id, model` with conditional `deleted_at` and `created_at` filters.

### `cms_get_fleet_stats_totals` — new

```diff
@@
+ CREATE OR REPLACE FUNCTION SCHEMA.cms_get_fleet_stats_totals(
+     p_include_deleted BOOLEAN, p_since TIMESTAMPTZ
+ ) RETURNS TABLE (
+     session_count INT, total_snapshot_size_bytes BIGINT,
+     total_tokens_input BIGINT, total_tokens_output BIGINT,
+     earliest_session_created_at TIMESTAMPTZ
+ )
```

- Totals across all sessions with same filters as `_by_agent`.
- Also returns `MIN(created_at)` as earliest session timestamp.

### `cms_upsert_session_metric_summary` — new

```diff
@@
+ CREATE OR REPLACE FUNCTION SCHEMA.cms_upsert_session_metric_summary(
+     p_session_id TEXT, p_updates JSONB
+ ) RETURNS VOID
```

- Accepts a JSONB object with increment fields (e.g. `tokensInputIncrement`, `dehydrationCountIncrement`).
- INSERT...ON CONFLICT: inserts with initial values on first call, increments counters on subsequent calls.
- Boolean flags (`lastDehydratedAt`, `lastHydratedAt`, `lastCheckpointAt`) set timestamps to `now()` when true.
- `snapshotSizeBytes` is an absolute set (not an increment).

### `cms_prune_deleted_summaries` — new

```diff
@@
+ CREATE OR REPLACE FUNCTION SCHEMA.cms_prune_deleted_summaries(
+     p_older_than TIMESTAMPTZ
+ ) RETURNS BIGINT
```

- Hard-deletes summary rows where `deleted_at IS NOT NULL AND deleted_at < p_older_than`.
- Returns the count of deleted rows.
