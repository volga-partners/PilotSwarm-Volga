{
  "name": "pilotswarm-web",
  "version": "0.1.0",
  "private": true,
  "description": "Web portal for PilotSwarm — hosts the full TUI in the browser via xterm.js + node-pty.",
  "type": "module",
  "scripts": {
    "build": "echo 'pilotswarm-web: no build step'",
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "node-pty": "^1.2.0-beta.12",
    "ws": "^8.18.2"
  },
  "devDependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/addon-web-links": "^0.12.0",
    "@xterm/xterm": "^6.0.0"
  }
}
